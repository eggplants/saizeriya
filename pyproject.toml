[build-system]
build-backend = "hatchling.build"
requires = [ "hatchling", "uv-dynamic-versioning" ]

[project]
name = "saizeriya"
description = "Unofficial Saizeriya Client"
readme = "README.md"
keywords = [
  "cli",
  "japanese",
  "menu",
  "restaurant",
  "saizeriya",
  "scraping",
  "textual",
  "tui",
]
license = "MIT"
authors = [ { name = "eggplants", email = "w10776e8w@yahoo.co.jp" } ]
requires-python = ">=3.14,<4"
classifiers = [
  "Development Status :: 3 - Alpha",
  "Environment :: Console",
  "Intended Audience :: End Users/Desktop",
  "License :: OSI Approved :: MIT License",
  "Natural Language :: Japanese",
  "Operating System :: OS Independent",
  "Programming Language :: Python :: 3 :: Only",
  "Programming Language :: Python :: 3.14",
  "Topic :: Internet :: WWW/HTTP",
  "Topic :: Utilities",
  "Typing :: Typed",
]
dynamic = [ "version" ]
dependencies = [
  "beautifulsoup4>=4.12",
  "httpx>=0.28.1",
  "lxml>=5",
]
optional-dependencies.tui = [
  "pillow>=11",
  "textual>=6",
  "textual-serve>=1.1.3",
  "zxing-cpp>=2.3",
]
urls.Documentation = "https://egpl.dev/saizeriya/"
urls.Issues = "https://github.com/eggplants/saizeriya/issues"
urls.Repository = "https://github.com/eggplants/saizeriya"
scripts.saizeriya = "saizeriya.cli:main"
scripts.saizeriya-mock-server = "saizeriya._mock:main"

[dependency-groups]
dev = [
  "pymarkdownlnt>=0.9.33",
  "pyproject-fmt>=2.11.1",
  "pytest>=9.0.2",
  "pytest-asyncio>=1.3",
  "pytest-cov>=7",
  "saizeriya[tui]",
]
docs = [
  "pdoc>=16",
]
mock = [
  "python-barcode>=0.16.1",
  "python-multipart>=0.0.27",
  "starlette>=1",
  "tdom>=0.1.15",
  "uvicorn>=0.46",
]

[tool.hatch]
version.source = "uv-dynamic-versioning"
build.targets.wheel.include = [ "saizeriya" ]
build.targets.sdist.include = [ "saizeriya" ]

[tool.uv]
python-downloads = "never"
default-groups = [
  "dev",
  "docs",
  "mock",
]
exclude-newer = "P7D"
preview = true

[tool.ruff]
line-length = 120
format.indent-style = "space"
format.quote-style = "double"
lint.select = [
  "ALL",
]
lint.ignore = [
  "COM812",
  "D203",
  "D213",
]
# Frozen into the binary by PyInstaller; not importable package content.
lint.per-file-ignores."packaging/entrypoint.py" = [
  "INP001",
]
lint.per-file-ignores."saizeriya/shops.py" = [
  "RUF001",
]
# Japanese UI copy deliberately uses full-width punctuation and signs.
lint.per-file-ignores."saizeriya/tui/screens/*.py" = [
  "RUF001",
]
lint.per-file-ignores."tests/test_*.py" = [
  "D",
  "S101",
]
lint.pydocstyle.convention = "google"

[tool.uv-dynamic-versioning]
vcs = "git"
style = "semver"
fallback-version = "0.0.0"
